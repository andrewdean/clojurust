[package]
name = "cljrs-bencode"
version = "0.1.16"
edition = "2024"
description = "Minimal incremental bencode codec shared by the nREPL server and the babashka pod protocol client"
license.workspace = true
repository.workspace = true

[dependencies]
thiserror = { workspace = true }
